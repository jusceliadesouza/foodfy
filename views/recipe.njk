{% extends 'layout.njk' %}

{% block head %}
    <title>Foodfy | {{recipe.title}}</title>
    <link rel="stylesheet" href="/styles/recipes.css">
{% endblock %}

{% block content %}
    <section class="content">
        <div class="recipe">
            <div class="image">
                <img src="{{recipe.image}}" alt="{{recipe.title}}">
            </div>

            <div class="info">
                <div class="header">
                    <h1 id="title">{{recipe.title}}</h1>
                    <p id="chef">por {{recipe.author}}</p>
                </div>

                <div class="details">
                    <div class="detail-title">
                        <h2>Ingredientes</h2>
                        <a class="detail-show">ESCONDER</a>
                    </div>

                    <div class="detail-display">
                        <ul class="dash">
                            {% for recipe in recipe.ingredients %}
                                <li>{{recipe}}</li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>

                <div class="details">
                    <div class="detail-title">
                        <h2>Modo de preparo</h2>
                        <a class="detail-show">ESCONDER</a>
                    </div>

                    <div class="detail-display">
                        <ul class="dash">
                            {% for recipe in recipe.preparation %}
                                <li>{{recipe}}</li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>

                <div class="details">
                    <div class="detail-title">
                        <h2>Informações Adicionais</h2>
                        <a class="detail-show">ESCONDER</a>
                    </div>

                    <div class="detail-display">
                        <p>{{recipe.information}}</p>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}