<form class="search" action="/" method="GET">
  <input 
      type="text" 
      name="filter"
      placeholder="Busque uma receita"
      {% if search.term %}
        value="{{search.term}}"
      {% endif %}
    >

  <button type="submit">Buscar</button>
</form>